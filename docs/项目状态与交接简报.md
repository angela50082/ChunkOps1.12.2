# ChunkOps112 · 项目状态与交接简报

> **面向新会话/新接手者**：读完本文 + `docs/设计文档.md` 即可无缝继续开发。
> 更新：2026-09-02（**全部定案**：存储表语义修复 ✓；“方块被替换”=换 jar 引起注册表重排（不动文件=稳定 468/7488，实测两次一致）+ 精确层旧缓存假象（已修）；最终规矩=mods 目录不许动 + 源区 Forestry/enderio 族方块在定型会话重放一遍）

## 0. 当前最重要的事（新会话第一任务！）

**✅ 2026-09-02 定案（全部真相，勿再折腾）**：

1. **存储表语义**：`storageStateId` = `Block.BLOCK_STATE_IDS.get(st)`（提交 9100fb1，已验证生效）。
2. **"方块被替换"完整因果链**：REID 环境部分模组（Forestry/enderio/quark/玄钢类）的注册表编号会随 **mods 目录内容变化而重排**；今天多次换 jar 导致每次启动编号都变（fence id 477→510→486→477→468）→ 旧会话写入的数据被新会话注册表重新解释（±1-2 家族漂移/变空气/变变体）→ 游戏内"被替换"。**对照实验（用户实测）：不动任何文件连续两次重启 = 编号完全一致（468/7488）→ 注册表在不扰动时稳定！**
3. **显示层（2026-09-02 定案，提交 f258b5f）**：地图渲染 = **文件级（磁盘解码）唯一显示**：`MapColor × 生物群系调色(草/叶/水) × 高度+坡度明暗`，全世界即时；加**持久渲染缓存 `MapTileCache`**（每 chunk bake 256 最终像素落盘 `chunkops/cache/<世界名>/<维度>/`，按 region 头时间戳失效）；**精确层(COPM)已退役并于 2026-09-15 彻底删除代码**（采集器/格式/相关工具全部移除，需要时从 git 历史取回）。取色铁律：从 palette 项重建状态必须用 `Block.BLOCK_STATE_IDS.getByValue`，勿用 Forge 重定义的 `getStateById`；透明方块(MapColor=0)回退纹理色。
4. **最终规矩**：mods 目录不许再动（动=重排）；jar 已定型；源区 Forestry/enderio 族方块在 468 族会话里重放一遍后，跨会话复制粘贴全部正确。
5. **多维显示 + 渲染性能（2026-09-15 提交 4375290，⚠️ 未安装——用户自行安装）**：编辑器顶部「维度」下拉（自扫 `region/` + `DIM<id>/region/`：下界 -1 / 末地 1 / 模组维度）；地图与复制/粘贴/统计/移除/清空/剪裁**全部作用于当前维度目录**；缓存按 `<世界名>/<维度>` 分目录。性能：地图色块缓存（视图不变不重建）、absent 不画占位、单色 tile 单 quad、每 chunk 只开一次 region 文件、拖动隔帧重建、删死纹理路径。**修复缓存路径 bug**：非主世界 dimDir 多一层导致 gameDir 推导错落 `saves/chunkops`（新增 `gameDirOf(dimDir,dim)`；旧错位目录可直接删）。当前跑测试的实例 = `E:\mc\HMCL\.minecraft\versions\1.12.2-Cleanroom`。
6. **新增入口：模组列表「配置」（2026-09-15 提交 2dc0e77，⚠️ 未安装）**：主菜单美化模组（FancyMenu/CustomMainMenu 等）会隐藏/清除注入到主菜单的按钮 → 改用 Forge 标准入口 `IModGuiFactory`（`ChunkOpsGuiFactory` + `ChunkOpsConfigScreen`，`@Mod(guiFactory=...)`）：**模组列表 → ChunkOps → 配置 → 打开区块编辑器**，不受主菜单美化影响。同时显示名改为 **`! ChunkOps 区块编辑器`**——Forge 模组列表按 `stripControlCodes(name).toLowerCase()` 排序（`GuiModList$SortType.NORMAL/A_TO_Z`），`!` 排在字母数字前，故**恒列最前**便于查找。
7. **界面全量本地化 i18n（2026-09-15）**：改造前**全部 UI 文案硬编码中文**（实测：`assets/` 不存在、`I18n` 使用 0 处、中文字面量 135 处）。现改为 Forge 标准语言文件 `src/main/resources/assets/chunkops/lang/{zh_cn,en_us}.lang`（**94 个键**，命名 `chunkops.<区域>.<名称>`），代码统一 `net.minecraft.client.resources.I18n.format(...)`：界面跟随**游戏语言**，非中/英语言回退 `en_us`。**两个坑（已处理，勿回退）**：① `TOOL_LABELS` 静态数组 / `statsSummary` 字段初始化发生在语言资源加载前 → 改为 `toolLabel(i)` 方法 + `statsSummary = null` 惰性取文案（绘制时回退 `chunkops.stats.hint`）；② 1.12.2 的 `@Mod(name)` 与 `mcmod.info` **不支持语言键** → 模组显示名固定英文 **`! ChunkOps`**（`!` 前缀仍是列表排最前的机制）。控制台日志（`[ChunkOps-diag]`、registry-snapshot 导出）**保持中文不动**，便于排障对照。新增键时 `en_us.lang` 与 `zh_cn.lang` 必须同步，否则英文玩家看到键名原文。

**证据链（勿重新调查）**：
- 快照/磁盘取证：新地图 5 region 全 0 unknown；粘贴区块（测试 (0,0)(1,0)(0,1)(1,1)）= JEID Palette int[] + 与粘贴会话快照一致的编号（源与磁盘均正确）
- 用户对照表（16:13 粘贴 → 16:39 会话查看）：抗燃木台阶 4510→高槿树台阶 4508、木板 4493→4492、栅栏 4516→4515、脉冲合金块 11481/2→玄钢梯子 11493、玄钢/磁钢→空气、合金单元 16701→16625 —— 全部=跨会话家族漂移特征
- 历次 fence id（对应每次 jar 更新）：13:32 前=449/475、14:38=526、15:13=477、15:44=510、16:13=486、16:39=477、**定型后=468（两次一致）**
- 反汇编三件套：BLOCK_STATE_IDS（field_176229_d，MCP 名 BLOCK_STATE_IDS，public，值=id<<4|meta）；Forge 重定义 getStateId=id+meta<<12 为旧错位根因；REID 2.3.0=JEID mixins（写盘=get / 读=getByValue）
- 教训固化：任何"存储值语义"结论必须【写盘字节码+真实存盘值+运行时映射名】三件套验证；Forge 补丁可能重定义同名 API；改 mods 目录会触发注册表重排（本环境实测）
- 补充（历史排障记录，仅存档）：Forge 重定义的 getStateId=id+meta<<12 曾导致 id<4096 全错（双重错误抵消"看起来正常"）；旧反射候选名在 MCP 运行时不存在；这些已由 9100fb1 修复，勿回退。

## 1. 项目是什么

1.12.2 模组兼容区块编辑器（对标 MCA Selector）：主菜单「区块编辑器」→ MCA 式 2D 地图 GUI，支持区块删除/清空/剪裁/统计/复制粘贴（跨整合包经名称化重映射不串模组块）。
技术栈：**Minecraft 1.12.2（Forge）+ JDK8 + ForgeGradle 2.3.4 + Gradle 4.9 + stable_39**（DataVersion 1343）。

## 2. 当前进度

| 阶段 | 状态 |
|---|---|
| 0-2（格式/数据层/重映射引擎） | ✅ 完成 |
| 3 主菜单 GUI（地图/框选/统计/复制粘贴/MCA 预览） | ✅ 完成，用户实测通过 |
| P2 精确数据层（COPM v2 采集+双层显示） | ✅ 完成（数据实时刷新/退出前同步） |
| P2 复制粘贴 | ✅ **全部完成并定案**（存储表语义 ✓；“被替换”=换 jar 触发的注册表重排+精确层旧缓存假象，均已定位/修复/给出最终规矩，见 §0） |

**已修复的全部关键问题（勿回退）**：
1. **颜色通道铁律**：DynamicTexture=ARGB 直填；sprite 帧=ABGR（R=低字节/G=(>>8)/B=(>>16)）；shade 0.92+0.08*y/255（显示端统一）
2. **DynamicTexture 在整合包暗化 ×0.533** → 地图改**顶点色批渲染**（POSITION_COLOR，drawTileQuads 带 blend、12000 quad/批、tile 粒度 size/2 clamp 2..16 精确边界）
3. **REID 存盘格式**：Palette int[] + Blocks 12bit 压缩索引（`seq=(Blocks<<4)|Data半字节` → `Palette[seq]`）；decodeJeidIndex/encodePalette 实现；**不是** 1.13 blockStates，Blocks 不是直接索引
4. **光照**：section 必须 SkyLight/BlockLight（2048B，缺失游戏崩 `NibbleArray should be 2048`）→ encodeLegacy 默认填 + Mcops putLight
5. **名称化管道**：.mcops（方块 `name#meta`↔stateId、biome 名称化、TE/实体/LevelExtra/RootExtra 原样）；**快照 state 级**（name#meta→stateId）；nameToStateId 直接查完整键；unknown:N 保留原 stateId；导入后 TE/实体坐标平移（多方块机器卡死根因）；批量 region 写入+回读校验
6. 采集器三级刷新（BlockEvent/Explosion dirty→0.5s 重采；5 分钟兜底；FMLServerStopping 同步刷 dirty）
7. UI：右侧工具面板（按钮/缩放滑块/统计列表过滤排序滚动/快捷键 R C T S Ctrl+C V F1 ?）、存档下拉、左下角指向坐标、右键取消框选、MCA 两段式粘贴（Ctrl+V 预览→拖动定位→再按确认；ESC 取消）

## 3. 代码地图

```
tools/verifier/src/com/chunkops/core/   （纯 Java 数据层；模组内同样编译）
  Json/BlockRef/RegistrySnapshot/RemapEngine/Mcops/RegionWriter/SectionCodec
  SectionCodec: decode(JeidIndex 12bit 压缩 / 1.13 palette / legacy 分支) + encodeLegacy(id>4095→palette)
                + encodePalette(12bit 压缩) + encodeSection(jeid flag)
  Mcops: exportChunks(name#meta↔stateId 名称化) / importChunks(data, snap, report, jeid)
tools/verifier/src/com/chunkops/verify/   （保留的验证/排查工具）
  
  NamedRoundTripCheck/TeCoordCheck/FixJeidWorld
src/main/java/com/chunkops/            （Forge 模组）
  ChunkOpsMod        @Mod（guiFactory 入口 + 显示名 "! ChunkOps" 排最前）；init 导出快照 + [ChunkOps-diag] 诊断
  ChunkOpsExport     storageStateId() = Block.BLOCK_STATE_IDS.get(st)
  gui/ ChunkOpsGuiHandler（主菜单按钮）/ ChunkOpsGuiFactory + ChunkOpsConfigScreen（模组列表入口）/
       ChunkOpsEditorScreen / ChunkMapRenderer / MapTileCache / MapColorCache / GuiOps
src/main/resources/assets/chunkops/lang/  （界面文案：zh_cn.lang / en_us.lang，94 键；代码用 I18n.format）
```

> **2026-09-15 清理**：精确层(COPM)整套代码已删除（`exact/ExactMapCollector`、`core/ExactMapFile`、
> `verify/ExactMapDump`、`verify/MapRenderSim`、`test/ExactMapTest`），另删除已完成使命的一次性诊断工具
> （MakePaletteSample/PixelStats/PixelAreas/LoopCompare/SrcDstCompare/DumpHeader/LevelDatInspector）
> 与 mod 内死代码（`staticColorFor`/`shade`/`flatTile`/采集器注册与回调）。需要时从 git 历史取回。

## 4. 关键事实与踩坑（务必先读）

**1.12.2 存盘格式**（实测权威）：
- chunk 根 `{Level, DataVersion:1343, ForgeDataVersion, 模组根级标签}`；Level 含 ForgeCaps——复制/移动整份携带
- region 头部 location=((起始扇区<<8)|扇区数)+时间戳表；zlib 压缩
- **JEID/REID 环境**：section = `Blocks`(压缩索引高 8 位 byte[4096]) + `Data`(索引低 4 位半字节) + `Palette int[]`(stateId) + Y + 光照；无 Add
- **非 REID**：legacy（Blocks/Add/Data，index=y*256+z*16+x，x 偶=低半字节）
- **1.13 palette+BlockStates 打包**：兼容解码存在（非主路径）

**环境**：
- JDK8=C:\Gradle\jdk8；Gradle=C:\Gradle\gradle-4.9；构建：`$env:JAVA_HOME=...;$env:GRADLE_USER_HOME=E:\IDE\Intellij\ChunkOps112\.gradle-home; C:\Gradle\gradle-4.9\bin\gradle.bat build`
- forge 1.12.2-14.23.5.2847（唯一有 userdev）；产物 build/libs/chunkops-0.1.0.jar
- **写入 E:\mc（用户存档）需 danger-full-access 权限**（沙箱 workspace-write 只允许项目目录）
- 用户存档：GreedyCraft2.4.8.1（versions\…，REID 环境）；`saves\测试`（**void 世界**！spawn 附近）、`saves\地图`（**源 3×3 区 chunk (14..16,44..46)**）、`saves\你好`（15 万 sections）
- 快照：versions\GreedyCraft2.4.8.1\registry-snapshot.json（模组 init 导出）

**用户环境**：MCP 反混淆（LoliASM/Cleanroom/587 模组）、OptiFine U_G5、Java 25、HMCL 版本隔离；mods 路径 versions\GreedyCraft2.4.8.1\mods

## 5. 验证方法

```bat
:: verifier 编译
C:\Gradle\jdk8\bin\javac.exe -encoding UTF-8 -d tools\verifier\out (Get-ChildItem tools\verifier\src -Filter *.java -Recurse).FullName
:: 常用
java -cp tools\verifier\out com.chunkops.verify.PaletteCheck <worldDir>          （palette 越界/legacy 检查）
java -cp tools\verifier\out com.chunkops.verify.SectionRoundTripCheck <worldDir> （section 往返）
java -cp tools\verifier\out com.chunkops.verify.JeidEncodeCheck <worldDir>       （JEID 编码往返）
java -cp tools\verifier\out com.chunkops.verify.NamedRoundTripCheck <world> <snap> （名称化往返——unknown 保留会假 PASS，注意）
java -cp tools\verifier\out com.chunkops.verify.PasteScan <world> <snap> [region] [范围]
java -cp tools\verifier\out com.chunkops.verify.TeCoordCheck <region.mca> <idx>   （TE 坐标）
```
**操作任何用户存档前先复制备份**；RegionWriter.write 自动生成 .mcabackup（可用于恢复）。

## 6. 当前待办

- [ ] **（最高）用户做"定型"验证**：当前 468 族会话中把源区 Forestry/enderio 族方块重放一遍 → 保存 → 重启（diag 应仍为 468/7488）→ 复制粘贴 → 重启后进目标世界查看=一致 → 项目收尾
- [ ] 收尾清单：更新设计文档 §6.6/§7 状态、完整回归（编辑器/采集/统计/复制粘贴/只读/备份）
- [ ] 可选后续：P2 方块级替换/过滤、维度采集、多人开关
- [ ] **重要约定**：任何"修 jar 再装"都会触发注册表重排——**后续改 jar 前必须提醒用户该风险**（改完 jar 后旧世界数据需重放源区）

## 7. 重要参考

- 方案：`docs/设计文档.md`（§6.6 精确数据层；核心=名称化 canonical+重映射管线）
- 本文件 §0 为当前排障的完整证据链，新会话从那里开始
