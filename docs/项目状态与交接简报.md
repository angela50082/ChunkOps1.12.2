# ChunkOps112 · 项目状态与交接简报

> **面向新会话/新接手者**：读完本文 + `docs/设计文档.md`（方案）+ `report/格式验证报告.md`（实测）+ `docs/阶段1-环境准备指南.md`（构建）即可无缝继续开发。
> 更新：2026-08-31（阶段 3 收尾时）

## 1. 项目是什么

1.12.2 模组兼容区块编辑器（对标 MCA Selector）：**游戏主菜单加「区块编辑器」按钮 → MCA 式 2D 地图 GUI**，支持区块删除/清空/剪裁/统计/复制粘贴（跨整合包经名称化重映射不串模组块）。
目标版本 **Minecraft 1.12.2（Forge）**，技术栈 **JDK8 + ForgeGradle 2.3.4 + Gradle 4.9 + stable_39**（DataVersion 1343）。

## 2. 当前进度（关键！）

| 阶段 | 状态 | 验证 |
|---|---|---|
| 0 格式实测 | ✅ 完成 | 14 万+ chunk 无崩溃；清单 1-9 全实测（`report/`） |
| 1 数据层+区块操作 | ✅ 完成（P0 验收通过） | clear/remove/trim/copy/move/备份还原全实测，0 损坏 |
| 2 重映射引擎 | ✅ 完成（P1 验收通过） | 跨整合包复制演示（拔刀之旅→GreedyCraft）零冲突、324 模组方块完整迁移、三种冲突策略实测 |
| 3 主菜单 GUI | 🔄 **功能全部实现**；用户 4 轮实测反馈的 bug 已全部修复；**最新修复（HeightMap 语义 + 取色栈改进）等待用户第 5 次实测** | 构建 121KB；日志确认模组在 GreedyCraft2.4.8.1 正常加载 |

**下一步**：等用户实测确认地图颜色/闪烁正常 → 阶段 3 验收关闭 → 实施**P2 增强：精确数据层**（设计文档 §6.6，Xaero 式游戏内采集，用户已确认方向）或按用户新指示走。

## 3. 代码地图

```
tools/verifier/src/com/chunkops/verify/   （独立 CLI 工具链，纯 Java 8，离线可跑）
  NbtNode / NbtReader / NbtWriter    NBT 读写（兼容 gzip/zlib/raw）
  RegionReader                        region 读取（location 表 + 时间戳表）
  ChunkInspector                      结构检查/全量 scan（--scan、dump）
  BlockIdStats / HeightMapCheck       方块 ID 统计 / HeightMap 语义验证
  DumpHeader / NbtDump                诊断工具
  SnapshotTest / SnapshotStats        快照往返测试 / 快照统计
  ModListScanner                      整合包 mods 目录 mcmod.info 扫描
  ChunkOpsTool                        CLI 操作（stats/clear/remove/trim/copy/mcops-export/import/buckets）
  MakePaletteSample                   自造 palette 格式样本
tools/verifier/src/com/chunkops/core/   （纯 Java 数据层；模组内同样编译）
  Json / BlockRef / RegistrySnapshot / SectionCodec / RegionWriter / RemapEngine / Mcops
src/main/java/com/chunkops/            （Forge 模组）
  ChunkOpsMod          @Mod 入口；init 时导出 registry-snapshot.json
  ChunkOpsExport       注册表快照导出（遍历 Block/Biome.REGISTRY + fingerprint）
  gui/ChunkOpsGuiHandler    主菜单按钮注入（GuiScreenEvent.InitGuiEvent.Post，按钮 ID 0x434F）
  gui/ChunkOpsEditorScreen   编辑器主界面（存档切换/地图/框选/右键菜单/日志/只读）
  gui/ChunkMapRenderer       2D 地图（异步线程池 4 / tile LRU 16384 / 纹理 LRU 2048 / 世界隔离 key）
  gui/MapColorCache          取色（atlas sprite 3×3 平均 + 品红检测 + MapColor 兜底）
  gui/GuiOps                 GUI 操作封装（session.lock / 活注册表统计 / 复制粘贴）
```
构建：`build.gradle` 的 `sourceSets.main.java.srcDirs += 'tools/verifier/src'`，模组 jar 含全部工具链（~121KB）。

## 4. 关键事实与踩坑（务必先读）

**1.12.2 存盘格式（实测权威结论）**
- Section = legacy 数组：`Blocks` byte[4096] + `Add` byte[2048]（高 4 位 ID，id>255 时出现）+ `Data` byte[2048]（meta 半字节）；索引 `y*256+z*16+x`；**半字节顺序 x 偶=低 4 位**（NibbleArray 语义）
- **Palette+BlockStates 仅内存/网络，1.13 才写盘**（1.12 无存档样本出现过；读取端保留兼容——自造样本已验证解码）
- chunk 根：`{Level, DataVersion:1343, ForgeDataVersion, <模组根级标签如 cofhworld/forestry/Thaumcraft/MekanismWorldGen...>}`；Level 含 `ForgeCaps`；**复制/移动必须整份携带**
- region 头部：location 条目 = `(起始扇区<<8)|扇区数`；**第二张表是时间戳不是 size 表**；压缩全 zlib；chunk 数据 = 4B 长度 + 1B 压缩类型 + NBT
- **无复数 Heightmaps**，旧 `HeightMap`=int[256]（JEID 环境）或 byte？——**实测 JEID 环境 IntArray**；语义 = **最高非空气方块 y + 1**（HeightMapCheck 实证：70/69、71/70；个别列挖掘后过期 → 定位时从 heightMap-1 向下扫描兜底）
- `Biomes`：vanilla=byte[256]，**JEID 环境=IntArray[256]**（正常输出非工具破坏；biome ID 可>255）
- 结构方块文件：palette 名称化 `{Name,Properties}` + `ForgeDataVersion`（模组版本表）→ 跨模组集安全
- `.schematic`（Schematica）：数字 ID+meta（Materials:"Alpha"）→ 跨模组集必串

**JEID（Just Enough IDs）**
- GreedyCraft 类整合包核心（level.dat Version Name=`1.13-JEID`）；方块 ID 上限 **13313**（实测快照：jaopca:molten.zomril）；**id>4095 方块的存盘格式尚无真实样本**（SectionCodec palette 兼容路径已覆盖）

**环境/构建**
- JDK8 在 `C:\Gradle\jdk8`；Gradle 4.9 在 `C:\Gradle\gradle-4.9`；系统 JAVA_HOME 已失效
- **构建命令**：`JAVA_HOME=C:\Gradle\jdk8` + `GRADLE_USER_HOME=E:\IDE\Intellij\ChunkOps112\.gradle-home` + `C:\Gradle\gradle-4.9\bin\gradle.bat build`（已跑通）
- **forge 必须用 1.12.2-14.23.5.2847**（唯一带 forge-userdev.jar 的构建；2854/2860 无 → extractUserdev 404）
- Gradle 4.x 的 GRADLE_USER_HOME 指向不可写目录会报 native-platform.dll 失败
- libraries.minecraft.net 偶发握手重置 → 重试即可
- 沙箱：网络/权限需要 danger-full-access；PowerShell 写文件默认带 BOM（javac 会报错，用无 BOM UTF8）

**1.12.2 API 坑（MCP stable_39）**
- `Block.REGISTRY.getNameForObject(b)`（`getKey` 是 1.13+ 的名字）
- `Biome.REGISTRY` 同；`handleMouseInput`/`keyTyped` 声明 `throws IOException`
- `BufferBuilder` 在 `net.minecraft.client.renderer`（不是 vertex 子包）；`Tessellator.getBuffer()` 返回 BufferBuilder
- `BakedQuad` **没有** getU0/getV0（1.13+ 才有）
- `DynamicTexture` 像素是 **ABGR**（GL_RGBA 小端）——ARGB 直填会红蓝交换；转换 `(a<<24)|(b<<16)|(g<<8)|r`
- ResourceLocation 用 `toString()` 解析 modid（getResourceDomain 编译不过）
- 用户整合包（HMCL 版本隔离）：mods 在 `versions\GreedyCraft2.4.8.1\mods`（553 jars / 532 modids）；快照导出到 `versions\GreedyCraft2.4.8.1\registry-snapshot.json`

**用户环境**
- 1.12.2 测试存档：`E:\mc\GreedyCraft\.minecraft\versions\GreedyCraft2.4.8.1\saves\测试`（用户新建权威样本）、`你好`（8 万 chunk）、`E:\mc\HMCL\.minecraft\versions\拔刀之旅\saves\流易斯`
- 真实快照：`E:\mc\GreedyCraft\.minecraft\versions\GreedyCraft2.4.8.1\registry-snapshot.json`（**587 模组 / 13314 方块 / 339 生物群系**，fingerprint `272041d0...`）
- 用户有 `.minecraft\versions\GreedyCraft2.4.8.1\logs\latest.log` 可查运行时日志

## 5. 验证方法

```bat
:: 模组构建
set JAVA_HOME=C:\Gradle\jdk8
set GRADLE_USER_HOME=E:\IDE\Intellij\ChunkOps112\.gradle-home
C:\Gradle\gradle-4.9\bin\gradle.bat build

:: verifier 编译（独立工具链）
C:\Gradle\jdk8\bin\javac.exe -encoding UTF-8 -d tools\verifier\out (Get-ChildItem tools\verifier\src -Filter *.java -Recurse).FullName
:: 常用
java -cp tools\verifier\out com.chunkops.verify.ChunkOpsTool stats <世界目录>
java -cp tools\verifier\out com.chunkops.verify.ChunkOpsTool copy <src> <dst> <x1> <z1> <x2> <z2> --snapshot-src <快照> --snapshot <快照>
java -cp tools\verifier\out com.chunkops.verify.SnapshotStats <registry-snapshot.json>
```

**操作任何用户存档前务必先复制副本**（`test-data/`，已 gitignore），测试流程：副本 → 操作 → verifier 对比（stats/dump）→ 备份还原验证。

## 6. 当前待办

- [ ] **用户第 5 次实测**：最新修复版（HeightMap y+1 语义 + 取色栈 3×3/品红检测/MapColor 兜底）——确认地图颜色正常
- [ ] 阶段 3 验收关闭（用户确认后 → update_goal complete）
- [ ] **P2 增强：精确数据层**（设计文档 §6.6 Xaero 式采集：MapColor+BlockColors 运行时取色 + 自研 COPM 格式 + 双层显示）
- [ ] 后续阶段（设计文档路线图：阶段 4 伴侣工具等）

## 7. 重要参考

- 方案：`docs/设计文档.md`（v0.4，含 §6.6 精确数据层）
- 实测：`report/格式验证报告.md`（格式事实权威来源）
- 构建：`docs/阶段1-环境准备指南.md`
- git 历史 25+ 次提交（每个提交含阶段成果说明）
